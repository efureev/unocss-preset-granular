/**
 * Что bench-zero обязан НЕ содержать. Проверяется
 * `node scripts/verify-apps.mjs bench-zero`.
 *
 * У стенда нет ни одного `present`: он существует ради отсутствия. Как только
 * сюда просочится что-то из пакета или пресета, знаменатель замера перестанет
 * быть нулём — молча, потому что сборка от этого не краснеет.
 */
export default {
  purpose: 'нулевая отметка замера: ни байта из провайдера и пресета',

  absent: [
    {
      what: 'токены фундамента heavy-package',
      css: '--xh-',
    },
    {
      // Маркер — именно `--un-rotate` из preflight'а presetMini, а не общий
      // префикс `--un-`: его объявляет и сам `@unocss/reset`, который в
      // стенде стоит законно (он одинаков в обоих стендах и в дельту не
      // входит). Проверка на префикс краснела бы на пустом месте.
      what: 'preflight presetMini — признак того, что в стенде завёлся UnoCSS',
      css: '--un-rotate',
    },
    {
      what: 'структурные классы компонентов пакета',
      css: '.xh-',
    },
  ],
}
