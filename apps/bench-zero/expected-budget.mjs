/**
 * Ожидаемый бюджет bench-zero. Стенд нулевой, поэтому проверять здесь надо
 * не значения, а отсутствие ролей: как только рядом с `vue`/`reset`/`entry`
 * появится `granular`, `app` или `pkg` — знаменатель замера перестал быть нулём.
 */
export default {
  purpose: 'нулевая отметка: только vue, reset и код приложения',

  assets: {
    roles: ['vue', 'reset', 'entry'],
  },
}
