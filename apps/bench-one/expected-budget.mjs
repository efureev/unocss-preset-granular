/**
 * Ожидаемый бюджет bench-one. Проверяется
 * `node scripts/report-css-budget.mjs --strict`.
 *
 * Сверка идёт в ОБЕ стороны: пропавшее ожидаемое нарушение — такое же
 * расхождение, как появившееся новое. Метрика, которую никто не проверяет,
 * тихо перестаёт работать, и заметить это по зелёной сборке нельзя.
 */
export default {
  purpose: 'бюджет CSS/JS одного компонента поверх полного фундамента',

  safelist: {
    // Намеренная фикстура `heavy-package`: правила `shadow-legacy` UnoCSS не
    // порождает. Единственный вердикт метрики без оговорок — и единственный
    // её пример. Убрать запись значит лишить детектор мёртвых записей
    // всякой проверки.
    deadEntries: ['shadow-legacy'],
    // Ступени собираются как `p-${PAD_STEP[size]}` и целой строкой в бандле
    // не встречаются. Они НУЖНЫ; отчёт лишь честно говорит, что доказать это
    // по артефакту нечем.
    unproven: ['p-2', 'p-3', 'p-4'],
    // `border` компонент пишет и в safelist, и в разметке, которую видит
    // файловый скан: запись избыточна.
    redundantWithScan: ['border'],
  },

  tokens: {
    // Потребление без объявления и без fallback — всегда дефект.
    undeclaredNoFallback: [],
    // Каналов ЧЕТЫРЕ, но `html` этим стендом не задействован: в разметке нет
    // ни инлайн-стилей, ни `var()`. Требовать четвёртый значило бы завести
    // его искусственно.
    minChannels: 3,
  },

  assets: {
    roles: ['vue', 'reset', 'entry', 'granular', 'app', 'pkg'],
  },

  // Ориентиры, НЕ гейт: печатаются рядом с фактом, расхождение не роняет
  // прогон. Порога на байты в этом репозитории нет — см. README стенда.
  hints: { granularCssGzip: 1936, componentCostGzip: 4248 },
}
