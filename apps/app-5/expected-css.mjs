/**
 * Что app-5 обязан эмитить. Проверяется `node scripts/verify-apps.mjs app-5`.
 *
 * Темы: оба блока токенов уже в CSS, а переключение — попадание в нужный
 * селектор; манифест тем отдаёт рантайму именно селекторы.
 * Строки: три импортированных языка в бандле есть, четвёртый — нет.
 */
export default {
  purpose: 'манифест тем в рантайме и отсечение неиспользуемых языков',

  present: [
    {
      what: 'блок токенов темы light (селектор :root — активна по умолчанию)',
      css: ':root{--x-tokenized:red}',
    },
    {
      what: 'блок токенов темы dark — уже в CSS, а не генерируется в рантайме',
      css: '--x-tokenized:yellow',
    },
    {
      // Ровно тот селектор, который манифест отдаёт рантайму как
      // { type: 'attribute', name: 'data-theme', value: 'dark' }. Если
      // провайдер сменит селектор, а манифест не пересоберётся — переключение
      // молча перестанет работать, и упадёт эта проверка.
      what: 'селектор активации dark, обещанный манифестом',
      css: '[data-theme=dark]',
    },
    {
      what: 'класс компонента XTokenized, использующий токен темы',
      css: 'var(--x-tokenized)',
    },
  ],

  absent: [
    {
      what: 'CSS невыбранного XTest1',
      css: '.x-sp-test',
    },
  ],

  /**
   * Строки. Проверяется собранный JS — в CSS от них нет ни следа, и до сих пор
   * вся цепочка «контракт → манифест → импорты → бандл» не проверялась ничем.
   */
  js: {
    present: [
      // Три языка, которые приложение импортировало поимённо.
      { what: 'словарь en доехал до сборки', js: 'x-sp-i18n:title:en' },
      { what: 'словарь ru доехал до сборки', js: 'x-sp-i18n:title:ru' },
      { what: 'словарь pt-BR доехал до сборки', js: 'x-sp-i18n:title:pt-BR' },
    ],

    absent: [
      {
        // Главная проверка: `es` пакет объявляет, а приложение не импортирует.
        // Если он здесь — сработал агрегат `./i18n/all` вместо именованных
        // локалей, и отсечение языков не работает. Единственное место, где это
        // доказано сборкой, а не документацией.
        what: 'словарь незапрошенного es (и, через него, агрегат всех языков)',
        js: 'x-sp-i18n:title:es',
      },
    ],
  },
}
