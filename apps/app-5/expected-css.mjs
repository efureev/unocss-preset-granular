/**
 * Что app-5 обязан эмитить. Проверяется `node scripts/verify-apps.mjs app-5`.
 *
 * Суть приложения: сборка отдаёт рантайму МАНИФЕСТЫ. Для тем это значит, что
 * оба блока токенов уже в CSS, а переключение — попадание в нужный селектор.
 * Для строк — что адреса и имена экспортов доехали до бандла, запрошенные
 * языки импортированы, а незапрошенный отсечён.
 */
export default {
  purpose: 'манифесты сборки: темы в рантайме, строки пакетов и отсечение языков',

  present: [
    {
      what: 'блок токенов темы light (селектор :root — активна по умолчанию)',
      css: ':root{--x-tokenized:red}',
    },
    {
      what: 'блок токенов темы dark — уже в CSS, а не генерируется в рантайме',
      css: '--x-tokenized:yellow',
    },
    {
      // Ровно тот селектор, который манифест отдаёт рантайму как
      // { type: 'attribute', name: 'data-theme', value: 'dark' }. Если
      // провайдер сменит селектор, а манифест не пересоберётся — переключение
      // молча перестанет работать, и упадёт эта проверка.
      what: 'селектор активации dark, обещанный манифестом',
      css: '[data-theme=dark]',
    },
    {
      what: 'класс компонента XTokenized, использующий токен темы',
      css: 'var(--x-tokenized)',
    },
  ],

  absent: [
    {
      what: 'CSS невыбранного XTest1',
      css: '.x-sp-test',
    },
  ],

  /**
   * Строки. Проверяется собранный JS — в CSS от них нет ни следа, и до сих пор
   * вся цепочка «контракт → манифест → импорты → бандл» не проверялась ничем.
   */
  js: {
    present: [
      {
        // Подпуть, который пресет вывел из одного лишь `id` провайдера. Если
        // дефолт разъедется с `exports` пакета, сборка упадёт раньше — но если
        // манифест перестанет доезжать до бандла, поймает эта проверка.
        what: 'адрес барреля строк в манифесте',
        js: '"entry":"@feugene/simple-package/i18n"',
      },
      {
        what: 'подпуть-агрегат, посчитанный от entry',
        js: '"allEntry":"@feugene/simple-package/i18n/all"',
      },
      {
        // Запрошен `ru-RU`, а пакет отдаёт `ru`: связка обязана указывать на
        // объявленный тег, иначе сгенерированный импорт не срезолвится.
        what: 'связка ru: региональный запрос обслужен базовым языком',
        js: '"locale":"ru","exportName":"ru","serves":["ru-RU"],"via":"base"',
      },
      {
        // Тег `pt-BR` идентификатором быть не может — импорт идёт через `ptBR`.
        // Ровно это расхождение и разводит связку на `locale` и `exportName`.
        what: 'связка pt-BR: тег и имя экспорта — разные строки',
        js: '"locale":"pt-BR","exportName":"ptBR"',
      },
      {
        what: 'все запрошенные языки кем-то обслужены',
        js: '"unserved":[]',
      },
      { what: 'словарь en доехал до сборки', js: 'x-sp-i18n:title:en' },
      { what: 'словарь ru доехал до сборки', js: 'x-sp-i18n:title:ru' },
      { what: 'словарь pt-BR доехал до сборки', js: 'x-sp-i18n:title:pt-BR' },
    ],

    absent: [
      {
        // Главная проверка всей затеи: `es` объявлен пакетом, но приложением
        // не запрошен. Если он здесь — отсечение языков не работает, а это ровно
        // то, ради чего контракт несёт список локалей вместо одного агрегата.
        // Заодно это проверка на агрегат: импорт `./i18n/all` привёл бы `es` с
        // собой, даже если никто его не просил.
        what: 'словарь незапрошенного es (и, через него, агрегат всех языков)',
        js: 'x-sp-i18n:title:es',
      },
    ],
  },
}
