/**
 * Что bench-pruned обязан эмитить. Проверяется
 * `node scripts/verify-apps.mjs bench-pruned`.
 *
 * Стенд отличается от `bench-one` ровно одной строкой конфига
 * (`pruneTokens.mode: 'on'`), поэтому его ожидания — это список того, что
 * обрезка обязана СОХРАНИТЬ и что обязана УБРАТЬ. Каждая строка в `present`
 * закрывает случай, в котором обрезка ломается молча: сборка зелёная, а
 * переменная разрешается в пустоту.
 */
export default {
  purpose: 'обрезка токенов включена: что уцелело и что ушло',

  present: [
    {
      // Имя собирается в рантайме (`var(${zIndexVar})`), в исходниках
      // `var(--xh-z-dropdown)` нет ни разу. Держит канал `source-literal`.
      // Без него токен уходит, z-index разрешается в unset, и оверлей
      // уезжает под соседний слой — без ошибки и без падения тестов.
      what: 'токен с динамическим именем уцелел',
      css: '--xh-z-dropdown',
    },
    {
      // Читается из JS по имени (`getPropertyValue`), `var()` нет.
      what: 'токен, читаемый из JS по имени, уцелел',
      css: '--xh-alert-duration',
    },
    {
      // Объявлен ТОЛЬКО в светлой теме; ссылается на него токен, объявленный
      // ТОЛЬКО в тёмной. Множество сохранённых обязано быть глобальным по
      // темам — посчитанное потемно теряет эту пару.
      what: 'токен светлой темы, нужный только тёмной, уцелел',
      css: '--xh-fg-boost',
    },
    {
      what: 'парный ему токен тёмной темы уцелел',
      css: '--xh-elevated-fg',
    },
    {
      // Роль ссылается на роль: замыкание обязано пройти по ссылке.
      what: 'цепочка роль → роль уцелела',
      css: '--xh-invalid-brd',
    },
    {
      // Производная формула и её литеральный fallback объявлены в разных
      // блоках, второй — внутри at-rule, которого плоский разбор не видит.
      what: 'фолбэк производной роли внутри @supports уцелел',
      css: '@supports not',
    },
    {
      what: 'структура тем не тронута: тёмная тема на месте',
      css: 'data-theme=dark',
    },
    {
      what: 'base.css не обрезается — его правила на месте',
      css: 'box-sizing',
    },
  ],

  absent: [
    {
      what: 'сырая палитра, которую не берёт ни один выбранный компонент',
      css: '--xh-amber-500',
    },
    {
      what: 'токен компонента, которого нет в сборке',
      css: '--xh-table-head-bg',
    },
    {
      what: 'неиспользуемая ступень шкалы отступов',
      css: '--xh-space-0',
    },
    {
      what: 'неиспользуемые контейнеры',
      css: '--xh-container-lg',
    },
  ],
}
