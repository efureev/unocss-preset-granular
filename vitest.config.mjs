import { defineConfig } from 'vitest/config'

/**
 * Тесты корневых скриптов. Пакеты тестируются своими конфигами — этот
 * покрывает только `scripts/`.
 *
 * Измеритель, который врёт, хуже отсутствия измерителя, а врут в нём ровно
 * чистые функции: парсер объявлений, экстрактор классов с экранированием и
 * граф достижимости. Отделённые от чтения `dist`, они проверяются строками.
 */
export default defineConfig({
  test: {
    include: ['scripts/__tests__/**/*.test.mjs'],
    environment: 'node',
  },
})
