/**
 * NODE-entry провайдера — алиас браузерного.
 *
 * Пакет объявляет фундамент ССЫЛКАМИ на CSS, а читает их node-слой пресета,
 * поэтому node-специфики здесь нет. Entry сохранён как часть публичного API:
 * приложения подключают именно его.
 */
export * from './index'
// `export *` не пробрасывает default.
export { default } from './index'
